#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int a, b, v;
	scanf("%d %d %d", &a, &b, &v);
	int i = a - b;
	int j = v - a;
	printf("%d", j % i == 0 ? j / i + 1 : j / i + 2);
}
